

export interface Skill {
    skillId: number;
    experience: string; // Change to Experience enum if using it on the frontend
    technology: string; // Change to Technology enum if using it on the frontend
  }